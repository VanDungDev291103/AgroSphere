// Configuration environment
const config = {
  // API và endpoints
  API_URL: "http://localhost:8080/api/v1",
  SOCKET_URL: "http://localhost:8080",
  
  // Thời gian timeout cho API requests (ms)
  API_TIMEOUT: 15000,
  
  // Debug mode
  DEBUG: true,
};

export default config; 